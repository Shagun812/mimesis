[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mimesis"
version = "0.1.0"
description = "MIMESIS mechanistic interpretability experiment"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]